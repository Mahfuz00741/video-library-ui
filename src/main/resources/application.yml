

server:
  port: 9086

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/db_video_library
    username: postgres
    password: 123456
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres

jwt:
  secret: secretkey123